@model IEnumerable<Lab_rab_3._0_KhasanovaNG_BPI_23_01.Models.Vegetable>

<h2>Отсортированные овощи</h2>

<table class="table">
    <thead>
        <tr>
            <th>Название</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
            </tr>
        }
    </tbody>
</table>